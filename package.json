{
  "name": "ilp-packet-viewer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "body-parser": "^1.19.0",
    "express": "^4.16.4",
    "ilp-packet": "^3.0.8",
    "nodemon": "^1.19.0",
    "npm-run-all": "^4.1.5",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-json-view": "^1.19.1",
    "react-scripts": "3.0.0"
  },
  "scripts": {
    "start": "run-p start-dev-server start-dev-client",
    "start-dev-client": "react-scripts start",
    "start-dev-server": "NODE_ENV=development PORT=8080 nodemon --watch server --watch build server",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:8080",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}